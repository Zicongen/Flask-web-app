# MCPize Configuration
# Docs: https://mcpize.com/docs/mcpize-yaml

version: 1

name: "mail-summary-tracker"
slug: "mail-summary-tracker"

runtime: typescript

build:
  install: "npm ci"
  command: "npm run build"

start:
  type: http
  command: "node dist/index.js"
